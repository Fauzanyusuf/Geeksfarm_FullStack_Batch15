<main>
  <h1>This is <%= title %></h1>
  <p>Wew</p>

  <% if (data.length > 0) { %>

  <table class="table">
    <tr>
      <th>name</th>
      <th>phone</th>
    </tr>
    <% data.forEach(contact => { %>
    <tr>
      <td><%= contact.name %></td>
      <td><%= contact.phone %></td>
      <td><%= contact.email %></td>
    </tr>
    <% }) %>
  </table>
  <% } else { %>
  <p>Data tidak tersedia</p>
  <% } %>
</main>
